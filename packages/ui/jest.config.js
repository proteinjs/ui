module.exports = {
  roots: ['<rootDir>/test'],
  transform: {
    '^.+\\.tsx?$': 'ts-jest',
  },
  testRegex: '(/__tests__/.*|(\\.|/)(test|spec))\\.tsx?$',
  moduleFileExtensions: ['ts', 'tsx', 'js', 'jsx', 'json', 'node'],
  testEnvironment: 'node',
  // A fixed non-UTC zone (UTC+12) for every suite, set before the workers fork — see test/timezone.js.
  globalSetup: '<rootDir>/test/timezone.js',
};
